git add . 

git commit -m"MESSAGE"

git push


git pull (pour récupérer)


git branch "nom de la branche" (créer une branche)

git checkout "nom de la branche" (aller sur une branche)

git checkout -b "nom de la branche" (créer et aller sur la branche)

git branch (pour voir toutes les branches)

