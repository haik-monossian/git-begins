git add . 

git commit -m"MESSAGE"

git push


git pull (pour récupérer)


BRANCHES:
1/ git branch "nom de la branche" (créer une branche)

2/ git checkout "nom de la branche" (aller sur une branche)

2.1/ git checkout -b "nom de la branche" (créer et aller sur la branche)

3/ git push -u origin nom-de-la-branche

git branch (pour voir toutes les branches)

